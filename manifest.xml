<iq:manifest xmlns:iq="http://www.garmin.com/xml/connectiq" version="2">
    <iq:application entry="HelloWorldApp"
                    id="com.example.helloworld.longappidexample12345678901234"
                    launcherIcon="@Drawables.LauncherIcon"
                    minSdkVersion="1.2.0"
                    name="Hello World App"
                    type="watchface">

        <!-- Supported devices -->
        <iq:products>
            <iq:product id="vivoactive_hr"/>
            <iq:product id="fenix3"/>
            <iq:product id="vivoactive3"/>
            <iq:product id="fenix6"/>
        </iq:products>

        <!-- Permissions (empty for now since it's a simple app) -->
        <iq:permissions/>

        <!-- Supported languages -->
        <iq:languages>
            <iq:language>eng</iq:language>
        </iq:languages>

    </iq:application>
</iq:manifest>
